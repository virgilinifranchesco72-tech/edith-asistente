const $=s=>document.querySelector(s);const logEl=$('#log');const pad=n=>String(n).padStart(2,'0');function now(){const d=new Date();return `${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`}function addLog(text){const p=document.createElement('p');p.innerHTML=`<time>${now().slice(0,5)}</time> ${text}`;logEl.prepend(p);while(logEl.children.length>7)logEl.lastElementChild.remove()}function clock(){const d=new Date();$('#time').textContent=`${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`;$('#date').textContent=d.toLocaleDateString('es-AR',{weekday:'long',day:'2-digit',month:'long',year:'numeric'}).toUpperCase()}function fluctuate(){const c=Math.floor(35+Math.random()*22),m=Math.floor(48+Math.random()*20);$('#cpu').textContent=c+'%';$('#memory').textContent=m+'%';$('#cpuBar').style.width=c+'%';$('#memoryBar').style.width=m+'%'}function action(type){if(type==='activate'){$('#state').textContent='SISTEMA ACTIVADO';addLog('Protocolo EDITH activado.')}if(type==='status'){addLog('Estado nominal. Todos los sistemas online.')}if(type==='clear'){logEl.innerHTML='';addLog('Registro limpiado.')} }document.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>action(b.dataset.action)));const saved=localStorage.getItem('edithUser');if(saved)$('#userName').textContent=saved.toUpperCase();localStorage.setItem('edithUser',$('#userName').textContent.toLowerCase());clock();fluctuate();setInterval(clock,1000);setInterval(fluctuate,3500);